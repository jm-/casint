ClrText();
Locate(2, 4, "It works!");
Locate(2, 5, "-- press any key --");

// wait for enter key
O = 0; Prog("BATTLSUB");

// animate progress bar for a bit
O = 1; Prog("BATTLSUB");
O = 2; F = 30;
for (A = 1 to 100) {
    Prog("BATTLSUB");
    theta = GetKey();
}

ClrText();
Locate(3, 6, "done.");
